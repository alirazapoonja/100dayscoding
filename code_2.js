let moviename = 'Kaalia';
console.log(moviename.toLowerCase()); // Shows the name in all small letters
console.log(moviename.toUpperCase()); // Shows the name in ALL BIG LETTERS
console.log(moviename.charAt(0).toUpperCase() + moviename.slice(1).toLowerCase()); // Shows the name with the First Letter Big
export {};
